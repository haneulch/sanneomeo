# 공공데이터포털 인증키 — .env.local로 복사 후 발급 키 입력 (커밋 금지)
#   cp .env.example .env.local
# 포털 마이페이지의 "일반 인증키(Decoding)" 값을 사용할 것 (Encoding 키 아님).
# 하나의 키로 승인받은 모든 API 호출 가능.
# 서버(API Route)에서만 사용 — NEXT_PUBLIC_ 접두사 금지 (브라우저 노출 방지).
DATA_GO_KR_KEY=
